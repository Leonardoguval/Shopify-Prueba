<section class="velora-page-hero">
  <div class="velora-page-hero-inner">
    <p class="velora-eyebrow">Velora</p>
    <h1>{{ page.title }}</h1>
  </div>
  <style>
    .velora-page-hero { background: linear-gradient(135deg, #f7f5f1 0%, #efe8dc 100%); padding: 72px 24px; }
    .velora-page-hero-inner { max-width: 900px; margin: 0 auto; text-align: center; }
    .velora-page-hero h1 { font-size: clamp(2rem, 3.2vw, 2.8rem); margin: 8px 0 0; color: #1f2321; }
  </style>
</section>
